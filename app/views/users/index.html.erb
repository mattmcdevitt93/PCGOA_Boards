<div class='dashboard_content'>
Users Index, Admin only <%= link_to 'comments', comments_path %>

<% @user.each do |user| %>
  <div class='dashboard_index'>
    <%= user.id %> - <%= user.username %> - <%= user.email %> |  <%= link_to 'User Profile', user %>
    
<%= will_paginate @user %>

  </div>
<% end %>
</div>