<!DOCTYPE html>
<html>
<head>
  <title>PCGOABoards</title>
  <link href='https://fonts.googleapis.com/css?family=Ubuntu|Audiowide|Squada+One|Open+Sans' rel='stylesheet' type='text/css'>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<div class='container-fluid'>
  
  <div class='logo col-md-8'>
  <%= link_to 'PCGOABoards', root_path %>
          <span class="notice pull-right" id="notice">   <%= notice %></span>

  </div>

    <div class="login col-md-3">
      <% if user_signed_in? %>
      <li>
        Signed in as <%= current_user.username %>
      </li>
      <li>
        <%= link_to('Edit Account', edit_user_registration_path) %> | <%= link_to('Logout', destroy_user_session_path, :method => :delete) %>
      </li>
      <% else %>
      <li>
        <%= link_to('Login', new_user_session_path)  %>  
      </li>
      <li>
        <%= link_to('Register', new_user_registration_path)  %>
      </li>
      <% end %>
    </div>

    <div class='nav-bar col-md-12 c_primary'>
      <span class='nav-link'><%= link_to 'Home', root_path %></span>
      <span class='nav-link'><%= link_to 'Events', event_path %></span>
      <span class='nav-link'><%= link_to 'Contact', root_path %></span>
    </div>
<div class='index_content col-md-10'>

<%= yield %>
</div>
  <% if @side === true %>
    <%= render 'side_bar' %>
  <% end %>
<hr>
<% request.path %>

</div>
</body>
</html>
